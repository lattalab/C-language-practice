#include <math.h>
#define PI acos(-1)
#define circle(x) PI*x*x
#define rectangle(x,y) x*y
#define triangle(base,height) 0.5*base*height
#define SQUARE(x) x*x
#define CUBIC(x) x*x*x
#define AVERAGE(x,y) (x+y)/2.0
#define PRODUCT(x,y) x*y
